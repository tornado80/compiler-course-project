program test
var
	r: real;
	a,b,c : integer;
procedure pow (f,a : integer);
var
	i, pow : integer;
begin
	i := 1;
	pow := 1 ;
	while (i <= a) do
	begin
		i := i + 1;
		pow := f * pow
	end
end;
begin
	pow(2,5)
end